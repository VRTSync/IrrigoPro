Agreed — Option 2. Your read of the scaffolding constraint is correct: the artifacts skill explicitly forbids createArtifact({ artifactType: "react-vite" }) in this Repl, the standard scripts under .local/secondary_skills/migrate-to-multi-artifact/references/ are detect/copy only with no scaffold script, and #306's "scaffold is already applied" preamble confirms it's platform-managed. Don't waste cycles trying to make scaffold creation work as a task — it can't.
Two additions to the standalone task before you draft, and one clarifying question:
Addition 1 — fold the env var inventory into this task. Pre-flight step 2 from #306 (read .env.example, grep process.env. across server/ and shared/, grep import.meta.env. across client/ for VITE_* vars, produce the full list, confirm each is set in Replit secrets) does not require scaffold to exist. It can run now against the live root. Doing it here means the port task later starts with the inventory already done and verified — one less pre-flight item that could go sideways post-scaffold. The standalone task becomes: tag, snapshot, diff verify, env var inventory, app-still-runs check, report.
Addition 2 — surface any divergence between snapshot and live as a hard fail. diff -rq failing on any non-excluded difference must abort the task, not just warn. The whole point of the snapshot is parity; an incomplete backup is worse than no backup because it gives false confidence to the port task that consumes it.
Question I need answered before you draft. When the migration handshake fires later, what's its expected interaction with an already-existing .migration-backup/ directory? Three possibilities:

The handshake creates .migration-backup/ itself as part of moving code into artifacts. Our pre-creating it would conflict, get overwritten, or confuse the handshake.
The handshake expects .migration-backup/ to already exist (created by setup task or equivalent) and reads from it during the port. Our doing it ahead of time is exactly right.
The handshake is indifferent — .migration-backup/ is purely for the port task's pre-flight verification and the handshake doesn't touch it.

#306's pre-flight assumes .migration-backup/ exists when the port runs, which suggests #2 or #3, not #1 — but I want this confirmed by reading the actual handshake skill, not inferred. Check .local/skills/migrate-to-multi-artifact/SKILL.md or wherever the handshake's behavior is documented, and tell me which scenario is true. If it's #1, the standalone task design needs to change — we'd skip the snapshot here and let the handshake do it, and our standalone task becomes tag + env var inventory + app-still-runs only.
Once you've confirmed the handshake's behavior, draft the task and show me before running. Same review playbook as #306.