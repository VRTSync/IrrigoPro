Fix: controller zone counts overwritten to 12 (+ finish the #1608 convergence). All company-scoped.
1. Lazy-seed must carry real zone counts. In artifacts/api-server/src/routes/irrigation-profile-routes.ts (~line 183), the lazy-seed currently calls ensureIrrigationControllers(callerCompanyId, customerId, count, branchName) — passing only the controller count, so new irrigation_controllers get totalZones = null and the UI shows a default of 12. Change it to create one irrigation_controllers per legacy property_controllers row in that branch, with totalZones = that row's zoneCount and a name from its controllerLetter — reuse the exact same seed logic as the import-irrigation-profile-from-property-controllers migration (extract a shared function so the two paths can't diverge). Update storage.ensureIrrigationControllers to take the per-controller zone counts, not a bare count.
2. Repair migration, re-runnable from /admin/migrations. Update import-irrigation-profile-from-property-controllers.ts so its idempotency corrects rather than skips: for any irrigation_controllers whose totalZones is null/missing, backfill it from the matching property_controllers.zoneCount for the same (companyId, customerId, branchName, controllerLetter/name), and sync the irrigation_profile_zones placeholders (add/trim empty trailing zones only — never delete a zone that has data). check()/preview() show the 12→real-count changes; run() applies them; re-running after correction is a no-op. Confirm it appears and runs from the Super Admin Migrations page.
3. Stop masking a missing count as 12. Remove the silent ?? 12 / DEFAULT_ZONE_COUNT = 12 fallbacks (irrigation-controller-resolver.ts:59, irrigation-system-card.tsx:19/68) — show "not set" or require a count on create, so a missing value never renders as a fabricated 12.
4. Finish the #1608 convergence (if not already done): (a) the wet-check lazy bootstrap (ensurePropertyControllers, routes.ts:15600 and :16453) must create/seed irrigation_controllers (canonical), not property_controllers, so newly-bootstrapped branches don't re-diverge; (b) when totalZones decreases, only trim empty trailing irrigation_profile_zones — never delete zones with programs/run-times/notes.
Verify: Woodglenn shows its real counts (7/9/15/6/8/9/5/10/10/10/8), not 12; running the repair migration corrects already-seeded customers; re-running it is a no-op; legacy property_controllers still holds the source counts.