But there's one real ordering bug, and it's my fault in the original spec — flag it before Replit builds:
Slice 2 stamps estimates.originWetCheckId, but Slice 3 is where that column gets created. Slice 2 (#1304) says "stamps originWetCheckId = wc.id on the estimate," yet the schema migration that adds estimates.originWetCheckId is Slice 3 step 1 (#1305). So as written, Slice 2 writes to a column that doesn't exist yet — it'll fail, or Replit will silently skip the stamp and Slice 3's "estimate has originWetCheckId" test won't hold. I split the columns wrong in the spec.
The fix is clean: move the estimates.originWetCheckId column into Slice 2 (where it's first written), and leave only work_orders.originWetCheckId in Slice 3 (where the WO carry-through happens). Then each slice is self-consistent — Slice 2 creates and stamps the estimate column together; Slice 3 adds the WO column, the banners, the conversion copy, and the surfacing. Tell Replit exactly that.
Three minor notes, none blocking:
The repo keeps numbered SQL migration files (latest is 0008_approved_by_role.sql), so for prod consistency I'd add 0009/0010 SQL files alongside the drizzle-kit push rather than relying on push alone — match the existing 0004–0008 pattern.
Slice 1's relevant-files list includes CombinedReviewSurface.tsx, which is actually a Slice 2 file — harmless, just ignore it for Slice 1.
On the entry point, be explicit with Replit: the field-tech wet-check entry today is the customer picker at /wet-checks (CustomerPickerPage), then /wet-checks/c/:customerId/new. "Insert the mode selector at kickoff" needs to say where relative to that — either a new first screen before the picker, or a step right after customer selection. Otherwise Replit will guess, and it might bury the mode choice somewhere awkward.
Net: only one thing must change before building — move estimates.originWetCheckId from Slice 3 into Slice 2 so the stamp has a column to land in. Want me to write that as a two-line correction you can paste into the #1304/#1305 tickets?