Both confirmations are yes, with two small adds and one final question.
Confirmation 1 — smaller scope is correct. Anything the handshake redoes (snapshot, scaffold) does not belong here. You've drawn the right line.
Two additions worth folding in. Both are cheap and earn their keep when something goes wrong post-handshake:
a. DB table inventory. Before the handshake fires, capture the current Neon prod schema as a baseline: every table name, column count per table, and a hash or row count for the sensitive ones (session, billing_number_counters, photo storage tables, MFA tables). Dump this into the completion report. The port task's DB push runs against a branch DB per its pre-flight 3, but having a clean pre-handshake baseline of prod makes any post-port drift trivial to detect — you can diff the live prod schema against this baseline at any point and instantly see what changed.
b. .replit config baseline. The handshake will rewrite .replit (new workflows for the artifacts, port shifts, possibly a different run command). Capture the current .replit contents verbatim into the completion report. We don't yet know whether the handshake's .migration-backup/ includes .replit or just user code; documenting it ourselves removes that uncertainty.
Both are pure read operations — no mutation, no scaffold dependency, no risk to the live app. Add roughly 30 seconds each to the task.
Confirmation 2 — #306 reuse plan, yes, with one verification I want from you before we commit. You said: "you'd want me (or whoever's running at that point) to paste #306's hardened description into the new task's plan file before accepting the handshake." That's load-bearing for this whole approach. Confirm by reading the handshake skill that there is in fact a propose-and-review pause between handshake initiation and execution — i.e., when proposeMigration fires, it generates a proposed port task in editable state, and the user (or agent) can modify the plan before clicking accept, at which point the handshake then executes scaffold + backup + auto-runs the port. If instead the handshake executes the port immediately on initiation with no review gate, our reuse plan doesn't work and we need a different approach (probably: hardened text gets pre-positioned somewhere the handshake reads, or we don't use the handshake's port at all and propose a custom port task against the post-handshake scaffold).
Verify the review-gate exists, tell me what you found, then draft the standalone task. I'll review before you create.
